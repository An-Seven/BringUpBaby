
从浏览器地址输入url到显示页面的步骤
基础版本：
1. 浏览器根据请求的URL交给DNS域名解析，找到真实的IP，向服务器发起请求
2.服务器交个后台处理完成后数据，浏览器接收文件（HTML,JS。。。）
3.浏览器对加载到的资源（HTML,CSS,JS。。）进行语法解析，建立相应的内部数据结构（例如html的dom）
3.载入解析到的资源文件，渲染页面

Vue和React的区别
1.React采用特殊的JSX语法，Vue在组件开发中也推崇编写.vue特殊文件格式，对文件内容都有一些阅读，两者都需要编译后使用；
2.中心思想相同：一切都是组件，组件实例之间可以嵌套
3.都提供了合理的钩子函数，可以让开发者定制化去处理需求
4.都不内置列数AJAX,Route等功能到核心包，而是以插件的方法加载
5.在组件开发中都支持mixins的特性
***6.React采用的Virtual DOM会对渲染出来的结果做脏检查，Vue中在模板中提供了指令，过滤器等，可以非常方便，快捷的操作DOM

Vue路由的钩子函数
首页可以控制导航跳转，beforeEach,afterEach等


Vuex是什么？怎么使用？哪种场景使用它？
只用来读取的状态集中放在store中，改变状态的方式是提交mutations,这是个同步的事物，异步逻辑应该封装在action中


$route和$router的区别
$route是“路由信息对象”，包含path, params, hash, query, fullPath,matched, name等路由信息参数。而$router是“路由实例”对象包含了路由的跳转方法，钩子函数等